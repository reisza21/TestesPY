print("Vamos fazer uma PA!")

termo = int(input('Digite o termo: '))
razao = int(input('Digite a razão: '))

ts = -1
i = 0
while i != 10: 
    ts = termo + i * razao
    print( ts)
    i +=1
    
print("PA com termo: {} e razão: {}".format(termo, razao))
